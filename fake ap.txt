with all due respect gentlemen, this don't work. 
guess i need to setup more things than just stating sta.


#include <WiFi.h>

const char* ap_ssid = "ESP32_TEST_AP";
const char* ap_password = "12345678";

const char* sta_ssid = "phone's hotspot";
const char* sta_password = "PASSWORD";

void setup() {
  Serial.begin(115200);
  delay(1000);

  Serial.println("Starting ESP32 AP + Station...");

  WiFi.mode(WIFI_AP_STA);

  // Connect to router
  WiFi.begin(sta_ssid, sta_password);
  Serial.print("Connecting to router");

  while (WiFi.status() != WL_CONNECTED) {
    delay(500);
    Serial.print(".");
  }

  Serial.println("\nConnected to router");
  Serial.print("Router IP: ");
  Serial.println(WiFi.localIP());

  // Start AP
  WiFi.softAP(ap_ssid, ap_password);
  Serial.print("AP IP: ");
  Serial.println(WiFi.softAPIP());
}

void loop() {}

